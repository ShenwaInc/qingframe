**添加规则文件可帮助模型精准理解你的编码偏好，如框架、代码风格等**
**规则文件只对当前工程生效，单文件限制10000字符。如果无需将该文件提交到远程 Git 仓库，请将其添加到 .gitignore**

- 以Git仓库为应用模块、微服务、插件为单位，将所有报告、记录、总结文件放到应用模块的 docs 目录下，将生成的所有测试、调试文件放在应用模块的 test 目录下
- 避免生成冗长的报告、记录和总结文件;
