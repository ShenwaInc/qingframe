## 前言
第三方应用不需要实现任何功能逻辑，只需要定义应用信息即可，作用是打开该应用时直接跳转至符合单点登录规范的第三方平台链接。该链接可以在固定定义在应用模块源码内，也可以留空通过后台配置。查看单点登录规范：[单点登录服务流程](https://www.yuque.com/shenwa/qingwork-dev/co6qmcz2kicd9wms)

使用第三方应用可以实现轻如云系统与其他自有系统的无缝衔接，互连互通。



## 安装包
### 安装路径
轻如云系统的模块源码放在项目的` **/public/addons/** `目录下，以模块标识为文件夹的命名。

### 文件结构
```plain
public
├─ addons                                   
│  ├─ third_app                              应用标识(示例)
│  │  ├─ resource                           资源目录（示例，非必须）
│  │  │  ├─ icon.jpg                        应用图标（示例，非必须）
│  │  │  └─ ...
│  │  ├─ install.php                      	安装脚本文件（示例，非必须）
│  │  ├─ manifest.json                      描述文件
│  │  ├─ site.php                           应用主类文件
│  │  └─ uninstall.php                      卸载脚本文件（示例，非必须）
│  └─ ...                                   其他应用模块
```

### 描述文件（manifest.json）
模块的描述文件以JSON格式保存在模块根目录下的  `**manifest.json**` 文件，其固定格式如下：

```json
{
  "application": {
    "name": "轻如云演示站",
    "identifie": "qingframe_demo",
    "version": "1.0.1",
    "releasedate": "202303011001",
    "type": "third_party_application",
    "module_type": "2",
    "ability": "Your Module",
    "description": "Your Module",
    "author": "神蛙科技",
    "url": "https://www.gxit.org/",
    "logo": "/static/images/microserver.png"
  },
  "servers": [],
  "permissions": [
    {
      "name": "轻如云演示站",
      "route": "index",
      "subPerm": [
      ]
    }
  ],
  "install": {
    "content": "install.php",
    "drive": "php"
  },
  "upgrade": {
    "content": "upgrade.php",
    "drive": "php"
  },
  "uninstall": {
    "content": "uninstall.php",
    "drive": "php"
  }
}
```

#### 字段说明
描述文件的结构、规范与内置应用（参考文档：[规范](https://www.yuque.com/shenwa/qingru/dae7wpppwv8lhzrz#a0Gtn)）一致，其中`application.module_type`节点的值固定为2，表示第三方应用。

### 应用主类（site.php）
应用主类文件`**site.php**`内必须要定义应用主类（`<font style="color:#74B602;">class</font> Addons\<font style="color:#DF2A3F;">your_app</font>\site`），开发者可以在这个类编写任何方法逻辑。在命名文件、类名、方法名、路由时，请注意区分大小写，并注意遵循PSR-4规范。

第三方应用的主类文件必须继承第三方应用基类 `**App\Utils\QuickModule**` ，该基类主要通过`doWebIndex`方法实现轻如云系统与第三方应用的跳转和鉴权功能，也可以在应用主类中重写该方法实现跳转功能。

#### 源码示例
```php
<?php

namespace Addons\identifie;

use App\Utils\QuickModule;

class site extends QuickModule{

    public $WebIndex = '';		//默认入口链接，例如：https://example.com/sso 。如留空则表示通过后台配置该URL
    public $SsoMaster = true;	//是否使用系统级单点登录CODE，设置为 false 表示使用租户级别的单点登录 CODE


    //重写 doWebIndex 方法逻辑，非必要可不包含
    public function doWebIndex(){
        $config = $this->module['config'];		//获取当前应用的后台配置信息（区分租户）
        // dd($config);
        $url = getYourRedirectUrl($config['WebIndex']);
        return redirect($url);
    }
    
}


?>

```

#### doWebIndex()
第三方应用只提供一个默认的控制器方法`<font style="color:#DF2A3F;">doWebIndex()</font>`（入口路由： /console/m/<font style="color:#DF2A3F;">{module}</font>/index），该方法定义在模块主类继承的系统应用基类`**App\Utils\QuickModule**`内，用于实现轻如云系统与第三方应用的跳转和鉴权功能，默认情况下您无需对该方法做任何改动实现对应功能；前台由第三方应用实现，因此不接收入口路由之外的任何请求。

该方法将自动获取[单点登录code](https://www.yuque.com/shenwa/qingwork-dev/co6qmcz2kicd9wms#rbDqq)，然后把它和主类文件定义的`**$WebIndex**`变量拼接成第三方应用的自动登录入口，并自动跳转到该入口。该入口接收到code后，需要实现自动登录和鉴权等正确的响应流程，具体实现流程请查看[单点登录服务](https://www.yuque.com/shenwa/qingwork-dev/co6qmcz2kicd9wms#JALBT)的说明，或参考第三方应用示例。

一般情况下，您只需要定义模块主类的公共变量`**$WebIndex**`和`**$SsoMaster**`就可以完成平台对接到第三方系统的工作，只需要在第三方系统实现单点登录的鉴权功能即可实现轻如云系统与第三方系统的无缝对接。

#### 默认参数配置
+ **$WebIndex**

第三方系统入口链接，可携带参数跳转，不支持含#号的链接。该变量可以为空，如果为空则表示需要后台动态设置入口链接，可登录超管账号通过【平台管理】→【应用与服务】单独为每个平台设置。后台跳转到第三方应用时，第三方链接需要把获取的code通过单独登录接口请求鉴权信息。

+ **$SsoMaster**

请求鉴权的接口要求使用 `secret` 进行接口签名验证，该秘钥可通过单点登录服务获取和设置。

默认情况下，第三方应用使用的是系统唯一的`<font style="color:rgb(53, 53, 53);">masterSecret</font>`<font style="color:rgb(53, 53, 53);">进行签名验证，如果您需要每个平台单独使用不同的</font>`<font style="color:rgb(53, 53, 53);">secret</font>`<font style="color:rgb(53, 53, 53);">进行接口签名验证，可在 site.php 将公共变量</font>`**$SsoMaster**`<font style="color:rgb(53, 53, 53);">设为</font>`<font style="color:rgb(53, 53, 53);">false</font>`<font style="color:rgb(53, 53, 53);">即可。</font>

## 其它规范
除了安装包的规范外，没有任何约束，所有目录结构和文件以及额外的功能均由模块开发者自由设计。

